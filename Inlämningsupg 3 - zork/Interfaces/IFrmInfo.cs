namespace Inlämningsupg_3___zork
{
    internal interface IFrmInfo
    {
        void HideAllLocationMarks();
        void DisplayItemList(Character character);
        void DisplayCurrentLocationMark(Character character);

    }
}